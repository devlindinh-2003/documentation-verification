# ========================
# APP CONFIG
# ========================
# The port the NestJS server will listen on
PORT=8000
FRONTEND_URL=http://localhost:5001

# The public-facing URL of this backend, used by the mock vendor to send webhook callbacks
CALLBACK_URL=http://localhost:8000/documents/callback

# ========================
# DATABASE
# ========================
# PostgreSQL connection string for Drizzle ORM
DB_USER=postgres
DB_PASSWORD=user
DB_NAME=document_verification_db
DB_PORT=5433
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}

# ========================
# REDIS (BullMQ)
# ========================
# Redis connection details for BullMQ job queues
REDIS_HOST=localhost
REDIS_PORT=6379

# ========================
# AUTH
# ========================
# Secret key used to sign and verify JWT tokens
JWT_SECRET=YOUR_JWT_SECRET_HERE

# ========================
# WEBHOOK & EXTERNAL SERVICES
# ========================
# Secret key used to verify the HMAC signature of incoming webhooks
HMAC_SECRET=YOUR_HMAC_SECRET_HERE

# The URL of the external document verification vendor (mock)
MOCK_VENDOR_URL=http://localhost:8000/verify

# ========================
# SUPABASE STORAGE
# ========================
SUPABASE_URL=YOUR_SUPABASE_URL_HERE
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE
SUPABASE_BUCKET=verification-documents
